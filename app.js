const kelvin = 0; 

// i created a constant variable with the kelvin number 

var celsius = kelvin - 273; 

// i created a changing variable which called the celsius which did kelvin minus 273 

var fahrenheit = celsius * (9/5) + 32

// i created a variable for the fahrenheit that could change 

var fahrenheit = Math.floor(fahrenheit);

console.log(`The temperature is ${fahrenheit} degress Fahrenheit.`);
